#ifndef COMPILER_PARSER_H
#define COMPILER_PARSER_H

#include <vector>

#include "Token.h"

class Parser
{

public:
  //parser object
  Parser(vector<token> stream);
private:


};

#endif //COMPILER_PARSER_H