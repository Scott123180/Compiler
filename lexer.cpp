#include <iostream>
#include <vector>
#include <string>

using namespace std;

token genToken(string type, string data, int line);

int main()
{
  char eof;
  cin >> eof;

  while(eof != '$')
  {
    char curchar;
    cin >> curchar;
    
  }
  return 0;
}
